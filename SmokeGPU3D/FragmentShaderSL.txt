#version 450 core

out vec4 color;

in vec4 fragmentColor;

uniform bool colorMode;
uniform double alphaValue;

void main(){
	if(colorMode){
		color = vec4(1.0, 1.0, 1.0, 0.3);
		//color = vec3(1.0, 1.0, 1.0);
	} else {
		//color = vec4(fragmentColor, alphaValue);
		color = fragmentColor;
	}
}